# DocStore

## Usage

```bash
python main.py
```

This will:
1. Scan desktop course folders
2. Index all files into SQLite database
3. Generate an interactive HTML dashboard
4. Open the dashboard in your browser

## Refresh

Run `python main.py` again to re-scan and update the dashboard.

## Monitored Folders

Configure in `src/config.py`.
